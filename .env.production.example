# ============================================
# 프로덕션 환경변수 템플릿
# .env 파일로 복사 후 실제 값으로 변경하세요
# cp .env.production.example .env
# ============================================

# 도메인 설정
DOMAIN=phonegabi.com
ACME_EMAIL=admin@phonegabi.com

# GitHub 저장소 (GHCR 이미지 경로)
GITHUB_REPOSITORY=sseroong/again-new-phone

# PostgreSQL
DB_USER=phonegabi
DB_PASSWORD=<strong-random-password>
DB_NAME=phone_marketplace

# Redis
REDIS_PASSWORD=<strong-random-password>

# JWT
JWT_SECRET=<min-32-chars-random-string>
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# 토스페이먼츠
TOSS_CLIENT_KEY=live_ck_xxxxx
TOSS_SECRET_KEY=live_sk_xxxxx

# Traefik Dashboard 인증 (htpasswd 형식)
# 생성: echo $(htpasswd -nB admin) | sed -e 's/\$/\$\$/g'
TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$xxxxx
